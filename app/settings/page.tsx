import { RoutePanel } from '@/components/dashboard/route-panel';

export default function SettingsPage() {
  return (
    <RoutePanel
      title="Settings"
      description="Configure workspace defaults, preferences, and dashboard experience options."
    />
  );
}
